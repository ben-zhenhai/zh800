<!DOCTYPE html>
<html>
<head>
  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

  <title>Zhenhai Dashboard</title>

  <link rel="stylesheet" type="text/css" href="/assets/sementicUI/css/semantic.css">
  <link rel="stylesheet" type="text/css" href="/assets/custom/css/custom.css">

  <script src="/assets/lib/javascript/jquery-2.1.1.js"></script>
  <script src="/assets/sementicUI/javascript/semantic.js"></script>

</head>
<body>
  <div class="ui fixed transparent inverted main menu">
    <div class="container">
      <a href="/" class="item"><div class="ui header">Zhenhai Dashboard</div></a>
    </div>
  </div>
  <div class="one column ui page grid" style="padding-top:60px; padding-bottom:20px">
    <div class="column">
      <% if (flash && flash.err) { %>
        <div class="ui error message">
          <div><%= flash.err[0]["message"] %></div>
        </div>
      <% } %>
      <div class="ui icon info message">
        <i class="info icon"></i>
        <div class="content">
          <div class="header">重設您的密碼</div>
          <p>請填寫下列的表單，以重新設定您的密碼。</p>
        </div>
      </div>
      <div>
        <form class="ui form segment" action="/user/resetPassword/reset" method="POST" id="resetForm">
          <div class="field">
            <label>新的密碼</label>
            <div class="ui left labeld icon input">
              <input placeholder="請輸入您的密碼" type="password" name="password">
              <i class="lock icon"></i>
            </div>
          </div>
          <div class="field">
            <label>確認密碼</label>
            <div class="ui left labeld icon input">
              <input placeholder="請再次輸入您的密碼以便確認" type="password" name="confirmPassword">
              <i class="lock icon"></i>
            </div>
          </div>
          <input type="hidden" name="confirmCode" value="<%= confirmCode %>" />
          <input type="hidden" name="_csrf" value="<%= _csrf %>" />
          <input type="submit" class="ui right floated blue submit button" value="確認" />
        </form> 
      </div>

    </div>
  </div>
  <script>
    $(function() {
      var validations = {
        "password": {
          "identifier": "password",
          "rules": [{
            "type": "length[8]",
            "prompt": "密碼至少要八個字元"
          }]
        },
        "passwordConfirm": {
          "identifier": "passwordConfirm",
          "rules": [{
            "type": "match[password]",
            "prompt": "兩次輸入的密碼不符，請再次確認您要用的密碼"
          }]
        }
      }

      $("#resetForm").form(validations, {on: "blur", inline: true})
    })

  </script>
</body>

</html>
